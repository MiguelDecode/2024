const active = true;

let msg = "";

// if (active) {
//   msg = "Activo";
// } else {
//   msg = "Inactivo";
// }

// msg = active ? "Activo" : "Inactivo";
msg = active && "Activo";

console.log(msg);
